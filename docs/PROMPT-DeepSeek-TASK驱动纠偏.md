# DeepSeek harness · TASK 驱动纠偏 prompt

> 整段复制下面的代码块到当前仓库的 DeepSeek harness。它替代旧 `PROMPT-DeepSeek-文字生涯重构.md` 的直接 M0→M5 执行方式。报告和任务文件必须与代码一起可读；文件缺失时先定位或获取，不能凭本段概述猜实现。

```text
你在 cs-career-simulation 仓库中接续文字生涯重构。这次不是从上次暂停点继续补功能，而是先纠正当前实现中已经确认的架构偏移，再严格按任务书完成首版。

最终产品仍是 CS2 选手生涯模拟器：玩家从弱队起步，在正式比赛、队伍关系、承诺、舆论和去留中做选择；借鉴 NBA 2K 生涯的叙事节奏与职业成长。不能改成篮球游戏，也不能把它写成与模拟世界脱节的纯对话小游戏。前端沿用已批准的 Sondaven 叙事阅读感与 HLTV 赛事资料层级，具体按 24 号视觉规范，禁止自行换成通用管理后台。

先完整阅读，顺序如下：
1. AGENTS.md 的 Docs-First 与 §10 单智能体流程。
2. docs/analysis/25-DeepSeek偏移复审-2026-09-11.md。
3. docs/analysis/26-文字生涯纠偏TASK-2026-09-11.md。
4. docs/analysis/23-文字生涯实施方案-2026-09-10.md。
5. docs/analysis/24-前端视觉与交互规范-2026-09-10.md。
6. docs/analysis/22-生涯叙事架构审查-2026-09-10.md 与当前任务所列子系统文档。
旧 PROGRESS-文字生涯-暂停点.md 仅供了解历史，不是验收凭据。

本次审查已经验证的事实：
- 最新工作树 cargo test 汇总 633 passed、0 failed、3 ignored；fmt 失败、Clippy 失败、wasm 编译通过。数字只作为起点，之后报告你实际执行的结果。
- M1 可靠提交没有完成。source 在应用前 reply Ok；request_id 缓存查找晚于 pending 校验、非持久、非权威、身份范围不足。
- 幂等测试允许相同请求返回 200 或 409，不能证明“返回原回执”。不允许继续保留这种放宽验收的写法。
- story 的 None timeout 仍然阻塞 recv，advance_unit 只是旧 advance 的包装；没有真实返回等待状态。
- snapshot.execution 是旧共享快照上的标记；remaining_steps=1、Month 被硬写。不得沿旧暂停记录继续实现“从月初回放恢复”。
- 剧情 next_scene 没进入运行时路径约束；PromiseDue 不检查到期时间；角色把普通队友当教练；效果会部分应用并产生与实际不符的说明。
- 当前剧情文件实际 3 弧 13 场景，不能照搬历史文档的 14。
- 新 /career 接线、web 前端和综合验收尚未完成。这是未完成，不要杜撰已有功能。
请先对照源码确认；若工作树已经变化，给出新证据更新判断，不机械修改已经修好的部分。

你的工作方式必须改变：整体规划 → 冻结不变量与反例 → 一个 TASK 的计划 → 实现 → 固定证据验收 → 下一个 TASK。

【第一阶段：只做 T00，然后 T01】
先读取 git 状态和相关 diff，保护所有用户未提交改动与已删除旧前端。
T00 期间不写生产代码、不扩写剧情、不创建功能前端。
生成 docs/DEV-文字生涯-整体纠偏计划.md 和 docs/DEV-文字生涯-TASK台账.md。
方案必须覆盖整条链路，不只是眼前报错：
1. 现有调用链、可复用类型/方法和保留/替换清单，精确到文件与函数。
2. 世界批次和正式比赛各暂停点的活变量、RNG 所有权、序列化 continuation、恢复入口。
3. 单局状态拥有者，step 返回后的命令循环，查询/保存/加载/关闭路径。
4. 全量验证、效果计划、应用、日志、落盘、回执的顺序；任何一步失败的处理。
5. request/generation/operation/batch 身份与幂等保留策略，正常重启和旧档回滚的差异。
6. v9 与当前 v10 草稿存档策略、内容版本策略、auto/CLI/WASM 兼容。
7. 剧情分支图、事实来源、演员绑定、真实效果和六页 API 契约。
8. T00–T12 的依赖、修改文件、验证场景和完成证据。
计划必须解释为什么不需要另一套引擎或另一套 DTO/规则。每个新增实现前先 rg 查同职责实现。

T01 建立 docs/DEV-文字生涯-验收矩阵.md，固定行为断言；重现 25 号探针反例。未来接口暂不存在时先记录精确测试规格，不引入无法编译的生产接口。第一阶段结束前明确输出整体计划摘要和设计闸门逐项结果。
G0 的必需产物自检满足后自主继续，不需要逐任务询问用户批准；不满足则补计划，不能边写生产边猜架构。

【执行阶段：严格使用 26 号任务卡】
顺序固定：T02 世界 step → T03 正式比赛 continuation → T04 服务端/暂停存档 → T05 事务/幂等 → T06 剧情图/时序 → T07 事实/演员/效果 → T08 一弧真实 API 闭环 → T09 三弧可达内容 → T10 页面计划/视觉基线 → T11 六页接线 → T12 综合验收。
WIP=1。只有前项 ACCEPTED 才启动下一项。不能因 T02 很难就先写页面或多写 JSON；不能跳过 T03 用独立 LiveSessionStore 冒充正式职业赛果。
每项开始先给：文档定位、源码事实、必复用 API、计划 diff 范围、输入输出、正反例验收、文档同步清单。以 26 号任务卡的完整要求为准，本 prompt 不构成缩减范围的理由。
每项完成更新台账，附真实命令/退出码、状态对比、文档与截图等证据。把未接线/未测/环境阻塞明确列出，不写“基本完成”代替状态。

【不可协商的不变量】
1. 等待玩家必须释放模拟线程；禁止无限 recv、暂停异常、旧快照叠 pending、整月重新回放。
2. 恢复不重抽候选、不重复地图/结算/经济/伤病、不多消耗 RNG；同种子同决策同内容版本的模拟结果不因暂停改变。
3. 全量前置验证通过后才应用；失败不留部分 world/RNG/journal/decision/scene/promise 状态。最终成功必须对应已持久提交的真实结果，入队仅能称 accepted。
4. 同身份同载荷在批次完成后和正常重启后返回同一 receipt；异载荷/错批次拒绝；并发只应用一次。旧 generation 不污染新运行实例。回执淘汰不把旧请求变新请求。
5. 静态 next_scene 必须成为有效运行路径；承诺根据模拟时间或正式比赛里程碑触发。不能点完一段对话就说又打完一场。
6. 不能让普通队友冒充教练，不能让当前战队全部历史比赛冒充主角参与史，不能让文字“转会成功”先于真实签约。结果由真实 AppliedOutcome 投影。
7. 内容校验必须拒绝重复 ID、坏引用和不合法出口；可达性用真实 driver/fixtures 证明，不能只统计 JSON 行数。
8. 新前端使用新 web/ 与 24 的栈/视觉 token。六页通过真实 DTO 接线，不在客户端计算默认决策、关系或赛果。不使用假数据冒充已接通的生产页面。
9. 保留共享 validator、已验证 LIVE pending 修复、有效纯值结构和现有规则。不得 reset/clean、覆盖用户修改或整体恢复已删除 frontend。
10. 任何 API/字段/版本改动同步对应 analysis 文档、SAVE-FORMAT、FRONTEND-API；计划与已实现必须分开标注。

【失败处理】
测试失败时先给出：完整首个真实错误、输入/预期/实际、根因、证据、最小修改范围、影响面、复验命令。区分逻辑、编译、环境问题。不得看到红灯立即补丁。
同一根因两次修复仍失败，先停止继续改该处，回到文档/源码重新规划并更新任务卡；没有新证据不能进行第三次补丁。有方案可自主继续，仅缺用户决策或外部能力时询问。
禁止删测/ignore、放宽断言、增加超时或 lint allow 掩盖问题、默认选项吞错误、扩大接口成功定义、伪造截图或通过记录。633 个通过也不是“测试数量必须凑到”的目标。
任务内只跑必要子集，影响面变大再扩展；已通过且无新改动不循环重跑全部门禁。验证用于核对既定设计，不用于随机寻找下一处修改。

【前端约束】
先完成 T10 页面/组件/状态映射和生涯页、世界页两个视觉基线，再 T11 接六页。
生涯页：人物处境、章节/日期/地点、可读正文、选择、真实结果是主内容；主 CTA 同时一个。
世界页：紧凑日期新闻、赛事结果行、排名表；数据支持剧情，不挤占阅读。
严格采用 24 的深墨蓝、暖纸、低饱和金色、字号、间距、按钮与焦点规则。禁止紫色渐变仪表盘、满屏等权重卡、过度圆角和装饰动画。
桌面 1440、手机 390 都检查 DOM 与截图；无读图能力按 AGENTS §8 的可用视觉桥，不虚报观察结果。参考站无法访问则诚实记录并按 24 继续，不自行杜撰它们的像素样式。

【最终交付】
完成 T12 的 Rust 四项门禁、实际前端 typecheck/build/关键交互测试、真实 HTTP 选择/保存/重启/恢复链路、3 弧可达性与六页截图。
输出 docs/DEV-文字生涯-验收报告.md、任务台账、文档变更、运行命令/URL/停止方式与残余问题。
所有结论必须可从源码、日志或截图核验。还有必需 TASK 未通过就明确未完成，不用“全部功能已实现，只剩测试”结束。

现在先读文档和当前 diff，执行 T00。你的第一份实质产出应当是整体纠偏计划，而不是代码补丁。
```
